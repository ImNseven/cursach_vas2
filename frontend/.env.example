# Скопируй в frontend/.env.local (для разработки) или frontend/.env.production (для прода).
# Оба файла в git не коммитятся.

# URL бэкенда.
# Для локальной разработки — http://localhost:8080
# Для Render — https://legal-analysis-backend.onrender.com
VITE_API_URL=http://localhost:8080

# GitHub OAuth Client ID (публичный, не секрет).
VITE_GITHUB_CLIENT_ID=Iv23litDJyhMjHkL1oDL
